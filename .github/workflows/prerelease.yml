name: Do PreRelease

on:
  push:
    paths:
      - "release/*"

jobs:
  do-prerelease:
    name: Do prerelease
    runs-on: ubuntu-22.04
    steps:
      - name: Pack & Release
        uses: bitwarden/dotnet-extensions/.github/workflows/pack-and-release.yml@main
        with:
          prerelease: true
