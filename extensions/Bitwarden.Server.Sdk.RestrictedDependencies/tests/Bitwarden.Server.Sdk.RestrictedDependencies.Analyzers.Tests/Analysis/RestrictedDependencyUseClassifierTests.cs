namespace Bitwarden.Server.Sdk.RestrictedDependencies.Analyzers.Tests.Analysis;

/// <summary>
/// Which of the five usage kinds a use of a restricted type takes, and the Record path every kind
/// ends in: AllowedPaths, the use rule, what is not a use at all, and the site a use is keyed to.
/// One partial file per kind, mirroring the one classifier that owns all five.
/// </summary>
public partial class RestrictedDependencyUseClassifierTests
{
}
